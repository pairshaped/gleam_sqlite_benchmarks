// This file is generated by Marmot. Do not edit by hand.
#![allow(dead_code)]
use rusqlite::{params, Connection, Result};

const COUNT_ADDONS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_addons where addonable_id = ?1 and addonable_type = ?2";

pub fn count_addons(
    conn: &Connection,
    event_id: i64,
    addonable_type: impl AsRef<str>,
) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_ADDONS_SQL)?;
    let rows = stmt.query_map(params![event_id, addonable_type.as_ref()], |row| row.get(0))?;
    rows.collect()
}

pub fn count_addons_one(
    conn: &Connection,
    event_id: i64,
    addonable_type: impl AsRef<str>,
) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_ADDONS_SQL)?;
    stmt.query_row(params![event_id, addonable_type.as_ref()], |row| row.get(0))
}

const COUNT_ADMIN_ALERTS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_admin_alerts where (country = ?1 or country is null) and (club_type = ?2 or club_type is null)";

pub fn count_admin_alerts(
    conn: &Connection,
    country: impl AsRef<str>,
    club_type: impl AsRef<str>,
) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_ADMIN_ALERTS_SQL)?;
    let rows = stmt.query_map(params![country.as_ref(), club_type.as_ref()], |row| {
        row.get(0)
    })?;
    rows.collect()
}

pub fn count_admin_alerts_one(
    conn: &Connection,
    country: impl AsRef<str>,
    club_type: impl AsRef<str>,
) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_ADMIN_ALERTS_SQL)?;
    stmt.query_row(params![country.as_ref(), club_type.as_ref()], |row| {
        row.get(0)
    })
}

const COUNT_CONFIG_PROBLEMS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_config_problems where club_id = ?1 and ignored = ?2";

pub fn count_config_problems(conn: &Connection, club_id: i64, ignored: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_CONFIG_PROBLEMS_SQL)?;
    let rows = stmt.query_map(params![club_id, ignored], |row| row.get(0))?;
    rows.collect()
}

pub fn count_config_problems_one(conn: &Connection, club_id: i64, ignored: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_CONFIG_PROBLEMS_SQL)?;
    stmt.query_row(params![club_id, ignored], |row| row.get(0))
}

const COUNT_CUSTOM_FIELDS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_custom_fields where club_id = ?1";

pub fn count_custom_fields(conn: &Connection, club_id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_CUSTOM_FIELDS_SQL)?;
    let rows = stmt.query_map(params![club_id], |row| row.get(0))?;
    rows.collect()
}

pub fn count_custom_fields_one(conn: &Connection, club_id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_CUSTOM_FIELDS_SQL)?;
    stmt.query_row(params![club_id], |row| row.get(0))
}

const COUNT_DISCOUNT_ITEMS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_discount_items where item_id = ?1 and item_type = ?2";

pub fn count_discount_items(
    conn: &Connection,
    event_id: i64,
    item_type: impl AsRef<str>,
) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_DISCOUNT_ITEMS_SQL)?;
    let rows = stmt.query_map(params![event_id, item_type.as_ref()], |row| row.get(0))?;
    rows.collect()
}

pub fn count_discount_items_one(
    conn: &Connection,
    event_id: i64,
    item_type: impl AsRef<str>,
) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_DISCOUNT_ITEMS_SQL)?;
    stmt.query_row(params![event_id, item_type.as_ref()], |row| row.get(0))
}

const COUNT_DISCOUNTS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_discounts where club_id = ?1 and active = ?2";

pub fn count_discounts(conn: &Connection, club_id: i64, active: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_DISCOUNTS_SQL)?;
    let rows = stmt.query_map(params![club_id, active], |row| row.get(0))?;
    rows.collect()
}

pub fn count_discounts_one(conn: &Connection, club_id: i64, active: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_DISCOUNTS_SQL)?;
    stmt.query_row(params![club_id, active], |row| row.get(0))
}

const COUNT_EVENT_CUSTOM_FIELDS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_event_custom_fields where event_id = ?1";

pub fn count_event_custom_fields(conn: &Connection, event_id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_EVENT_CUSTOM_FIELDS_SQL)?;
    let rows = stmt.query_map(params![event_id], |row| row.get(0))?;
    rows.collect()
}

pub fn count_event_custom_fields_one(conn: &Connection, event_id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_EVENT_CUSTOM_FIELDS_SQL)?;
    stmt.query_row(params![event_id], |row| row.get(0))
}

const COUNT_EVENTS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_events where club_id = ?1";

pub fn count_events(conn: &Connection, club_id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_EVENTS_SQL)?;
    let rows = stmt.query_map(params![club_id], |row| row.get(0))?;
    rows.collect()
}

pub fn count_events_one(conn: &Connection, club_id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_EVENTS_SQL)?;
    stmt.query_row(params![club_id], |row| row.get(0))
}

const COUNT_FEES_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_fees where club_id in (?1, ?2, ?3) and active = ?4";

pub fn count_fees(
    conn: &Connection,
    club_id: i64,
    parent_id: i64,
    grandparent_id: i64,
    active: i64,
) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_FEES_SQL)?;
    let rows = stmt.query_map(params![club_id, parent_id, grandparent_id, active], |row| {
        row.get(0)
    })?;
    rows.collect()
}

pub fn count_fees_one(
    conn: &Connection,
    club_id: i64,
    parent_id: i64,
    grandparent_id: i64,
    active: i64,
) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_FEES_SQL)?;
    stmt.query_row(params![club_id, parent_id, grandparent_id, active], |row| {
        row.get(0)
    })
}

const COUNT_PRODUCTS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_products where club_id = ?1 and active = ?2 and product_type in (?3, ?4)";

pub fn count_products(
    conn: &Connection,
    club_id: i64,
    active: i64,
    product_type_1: impl AsRef<str>,
    product_type_2: impl AsRef<str>,
) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_PRODUCTS_SQL)?;
    let rows = stmt.query_map(
        params![
            club_id,
            active,
            product_type_1.as_ref(),
            product_type_2.as_ref()
        ],
        |row| row.get(0),
    )?;
    rows.collect()
}

pub fn count_products_one(
    conn: &Connection,
    club_id: i64,
    active: i64,
    product_type_1: impl AsRef<str>,
    product_type_2: impl AsRef<str>,
) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_PRODUCTS_SQL)?;
    stmt.query_row(
        params![
            club_id,
            active,
            product_type_1.as_ref(),
            product_type_2.as_ref()
        ],
        |row| row.get(0),
    )
}

const COUNT_SPONSORS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_sponsors where club_id = ?1";

pub fn count_sponsors(conn: &Connection, club_id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_SPONSORS_SQL)?;
    let rows = stmt.query_map(params![club_id], |row| row.get(0))?;
    rows.collect()
}

pub fn count_sponsors_one(conn: &Connection, club_id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_SPONSORS_SQL)?;
    stmt.query_row(params![club_id], |row| row.get(0))
}

const COUNT_TAGS_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_tags where club_id = ?1";

pub fn count_tags(conn: &Connection, club_id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_TAGS_SQL)?;
    let rows = stmt.query_map(params![club_id], |row| row.get(0))?;
    rows.collect()
}

pub fn count_tags_one(conn: &Connection, club_id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_TAGS_SQL)?;
    stmt.query_row(params![club_id], |row| row.get(0))
}

const COUNT_TAXES_SQL: &str = r"-- returns: ValueRow
select count(*) as value from app_taxes where province = ?1";

pub fn count_taxes(conn: &Connection, province: impl AsRef<str>) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(COUNT_TAXES_SQL)?;
    let rows = stmt.query_map(params![province.as_ref()], |row| row.get(0))?;
    rows.collect()
}

pub fn count_taxes_one(conn: &Connection, province: impl AsRef<str>) -> Result<i64> {
    let mut stmt = conn.prepare_cached(COUNT_TAXES_SQL)?;
    stmt.query_row(params![province.as_ref()], |row| row.get(0))
}

const GET_BRANDING_PALETTE_ID_SQL: &str = r"-- returns: ValueRow
select id as value from app_branding_palettes where id = ?1";

pub fn get_branding_palette_id(conn: &Connection, id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(GET_BRANDING_PALETTE_ID_SQL)?;
    let rows = stmt.query_map(params![id], |row| row.get(0))?;
    rows.collect()
}

pub fn get_branding_palette_id_one(conn: &Connection, id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(GET_BRANDING_PALETTE_ID_SQL)?;
    stmt.query_row(params![id], |row| row.get(0))
}

const GET_CLUB_ID_SQL: &str = r"-- returns: ValueRow
select id as value from app_clubs where id = ?1";

pub fn get_club_id(conn: &Connection, id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(GET_CLUB_ID_SQL)?;
    let rows = stmt.query_map(params![id], |row| row.get(0))?;
    rows.collect()
}

pub fn get_club_id_one(conn: &Connection, id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(GET_CLUB_ID_SQL)?;
    stmt.query_row(params![id], |row| row.get(0))
}

const GET_CLUB_ID_BY_SUBDOMAIN_SQL: &str = r"-- returns: ValueRow
select id as value from app_clubs where subdomain = ?1";

pub fn get_club_id_by_subdomain(conn: &Connection, subdomain: impl AsRef<str>) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(GET_CLUB_ID_BY_SUBDOMAIN_SQL)?;
    let rows = stmt.query_map(params![subdomain.as_ref()], |row| row.get(0))?;
    rows.collect()
}

pub fn get_club_id_by_subdomain_one(conn: &Connection, subdomain: impl AsRef<str>) -> Result<i64> {
    let mut stmt = conn.prepare_cached(GET_CLUB_ID_BY_SUBDOMAIN_SQL)?;
    stmt.query_row(params![subdomain.as_ref()], |row| row.get(0))
}

const GET_EVENT_COUNTER_SQL: &str = r"-- returns: ValueRow
select counter as value from app_events where id = ?1";

pub fn get_event_counter(conn: &Connection, event_id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(GET_EVENT_COUNTER_SQL)?;
    let rows = stmt.query_map(params![event_id], |row| row.get(0))?;
    rows.collect()
}

pub fn get_event_counter_one(conn: &Connection, event_id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(GET_EVENT_COUNTER_SQL)?;
    stmt.query_row(params![event_id], |row| row.get(0))
}

const GET_EVENT_ID_SQL: &str = r"-- returns: ValueRow
select id as value from app_events where club_id = ?1 and id = ?2";

pub fn get_event_id(conn: &Connection, club_id: i64, event_id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(GET_EVENT_ID_SQL)?;
    let rows = stmt.query_map(params![club_id, event_id], |row| row.get(0))?;
    rows.collect()
}

pub fn get_event_id_one(conn: &Connection, club_id: i64, event_id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(GET_EVENT_ID_SQL)?;
    stmt.query_row(params![club_id, event_id], |row| row.get(0))
}

const GET_FEE_ID_SQL: &str = r"-- returns: ValueRow
select id as value from app_fees where id = ?1";

pub fn get_fee_id(conn: &Connection, id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(GET_FEE_ID_SQL)?;
    let rows = stmt.query_map(params![id], |row| row.get(0))?;
    rows.collect()
}

pub fn get_fee_id_one(conn: &Connection, id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(GET_FEE_ID_SQL)?;
    stmt.query_row(params![id], |row| row.get(0))
}

const GET_PRODUCT_ID_SQL: &str = r"-- returns: ValueRow
select id as value from app_products where id = ?1";

pub fn get_product_id(conn: &Connection, id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(GET_PRODUCT_ID_SQL)?;
    let rows = stmt.query_map(params![id], |row| row.get(0))?;
    rows.collect()
}

pub fn get_product_id_one(conn: &Connection, id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(GET_PRODUCT_ID_SQL)?;
    stmt.query_row(params![id], |row| row.get(0))
}

const GET_USER_ID_SQL: &str = r"-- returns: ValueRow
select id as value from app_users where id = ?1";

pub fn get_user_id(conn: &Connection, id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(GET_USER_ID_SQL)?;
    let rows = stmt.query_map(params![id], |row| row.get(0))?;
    rows.collect()
}

pub fn get_user_id_one(conn: &Connection, id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(GET_USER_ID_SQL)?;
    stmt.query_row(params![id], |row| row.get(0))
}

const SUM_PARENT_CHAIN_SQL: &str = r"-- returns: ValueRow
with recursive parents(id, parent_id) as (
    select id, parent_id from app_clubs where id = ?1
    union all
    select c.id, c.parent_id from app_clubs c inner join parents p on p.parent_id = c.id
)
select cast(coalesce(sum(id), 0) as integer) as value from parents";

pub fn sum_parent_chain(conn: &Connection, club_id: i64) -> Result<Vec<i64>> {
    let mut stmt = conn.prepare_cached(SUM_PARENT_CHAIN_SQL)?;
    let rows = stmt.query_map(params![club_id], |row| row.get(0))?;
    rows.collect()
}

pub fn sum_parent_chain_one(conn: &Connection, club_id: i64) -> Result<i64> {
    let mut stmt = conn.prepare_cached(SUM_PARENT_CHAIN_SQL)?;
    stmt.query_row(params![club_id], |row| row.get(0))
}

const UPDATE_EVENT_SQL: &str =
    r"update app_events set counter = counter + 1, name = ?1 where id = ?2";

pub fn update_event(conn: &Connection, name: impl AsRef<str>, event_id: i64) -> Result<usize> {
    conn.execute(UPDATE_EVENT_SQL, params![name.as_ref(), event_id])
}
